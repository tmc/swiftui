// Code generated by applegen; DO NOT EDIT.

import Charts
import Foundation

struct ChartSpec: Decodable {
    let marks: [MarkSpec]
    let xDomain: DomainSpec?
    let yDomain: DomainSpec?
    let xRange: PositionScaleRangeSpec?
    let yRange: PositionScaleRangeSpec?
    let xScaleType: ScaleTypeSpec
    let yScaleType: ScaleTypeSpec
    let xAxis: AxisConfigSpec?
    let yAxis: AxisConfigSpec?
    let xAxisLabel: AxisLabelSpec?
    let yAxisLabel: AxisLabelSpec?
    let xAxisStyle: AxisStyleSpec?
    let yAxisStyle: AxisStyleSpec?
    let styleScale: ForegroundStyleScaleSpec?
    let symbolScale: SymbolScaleSpec?
    let lineStyleScale: LineStyleScaleSpec?
    let symbolSizeScale: SymbolSizeScaleSpec?
    let legend: LegendConfigSpec?
    let plot: [PlotStyleSpec]?
    let scrollAxes: Int32
    let scrollPosition: ScrollPositionSpec?
    let visibleLength: Double?
    let xSelection: StateRefSpec?
    let ySelection: StateRefSpec?
    let xSelectionRange: StateRefSpec?
    let ySelectionRange: StateRefSpec?
    let angleSelection: StateRefSpec?
    let scrollTarget: ScrollTargetBehaviorSpec?
    let xScrollBinding: StateRefSpec?
    let yScrollBinding: StateRefSpec?
    let xVisibleLength: VisibleDomainLengthSpec?
    let yVisibleLength: VisibleDomainLengthSpec?
    let overlays: [ProxyLayerSpec]?
    let backgrounds: [ProxyLayerSpec]?
    let gesture: ProxyGestureSpec?
}

struct ValueSpec: Decodable {
    let kind: Int32
    let label: String
    let category: String
    let integer: Int
    let number: Double
    let timeUnixMS: Int64
    let timeUnit: Int32
}

struct DimSpec: Decodable {
    let role: Int32
    let value: ValueSpec
    let secondary: ValueSpec
    let floatV: Double
}

struct MarkSpec: Decodable {
    let kind: Int32
    let dims: [DimSpec]
    let mods: [MarkModSpec]
    let children: [MarkSpec]?
    let pointData: [PointPlotDatumSpec]?
    let rectangleData: [RectanglePlotDatumSpec]?
    let ruleData: [RulePlotDatumSpec]?
}

struct MarkModSpec: Decodable {
    let kind: Int32
    let colorR: Double
    let colorG: Double
    let colorB: Double
    let colorA: Double
    let label: String
    let value: String
    let floatV: Double
    let floatW: Double
    let floatX: Double
    let floatY: Double
    let intV: Int32
    let dash: [Double]?
    let viewPtr: String
}

struct ScaleTypeSpec: Decodable {
    let kind: Int32
    let value: Double
}

struct DomainSpec: Decodable {
    let kind: Int32
    let minInt: Int
    let maxInt: Int
    let minNumber: Double
    let maxNumber: Double
    let startUnixMS: Int64
    let endUnixMS: Int64
    let categories: [String]?
}

struct AxisValuesSpec: Decodable {
    let kind: Int32
    let step: Double
    let unit: Int32
    let count: Int
    let desiredCount: Int
    let roundLowerBound: Bool
    let roundUpperBound: Bool
    let values: [ValueSpec]?
}

struct AxisTickLengthSpec: Decodable {
    let kind: Int32
    let value: Double
}

struct LineStyleSpec: Decodable {
    let width: Double
    let dash: [Double]?
}

struct ValueFormatSpec: Decodable {
    let kind: Int32
    let precision: Int
    let unit: Int32
    let suffix: String
}

struct AxisConfigSpec: Decodable {
    let hidden: Bool
    let preset: Int32
    let position: Int32
    let values: AxisValuesSpec
    let showGrid: Bool
    let gridStyle: LineStyleSpec
    let gridCentered: Bool
    let showTicks: Bool
    let tickStyle: LineStyleSpec
    let tickCentered: Bool
    let tickLength: AxisTickLengthSpec
    let showLabels: Bool
    let datePattern: String
    let labelCentered: Bool
    let valueFormat: ValueFormatSpec
}

struct AxisLabelSpec: Decodable {
    let text: String
    let position: Int32
    let alignment: Int32
    let hasPosition: Bool
    let hasAlignment: Bool
    let hasSpacing: Bool
    let spacing: Double
}

struct AxisStyleEntrySpec: Decodable {
    let kind: Int32
    let colorR: Double
    let colorG: Double
    let colorB: Double
    let colorA: Double
    let opacity: Double
    let viewPtr: String
}

struct AxisStyleSpec: Decodable {
    let entries: [AxisStyleEntrySpec]?
}

struct PositionScaleRangeSpec: Decodable {
    let kind: Int32
    let startPadding: Double
    let endPadding: Double
}

struct StyleScaleSpec: Decodable {
    let key: String
    let colorR: Double
    let colorG: Double
    let colorB: Double
    let colorA: Double
}

struct ForegroundStyleScaleSpec: Decodable {
    let entries: [StyleScaleSpec]?
    let scaleType: ScaleTypeSpec?
}

struct SymbolScaleEntrySpec: Decodable {
    let key: String
    let symbol: Int32
}

struct SymbolScaleSpec: Decodable {
    let entries: [SymbolScaleEntrySpec]?
}

struct LineStyleScaleEntrySpec: Decodable {
    let key: String
    let style: LineStyleSpec
}

struct LineStyleScaleSpec: Decodable {
    let entries: [LineStyleScaleEntrySpec]?
}

struct SymbolSizeScaleEntrySpec: Decodable {
    let key: String
    let size: Double
}

struct SymbolSizeScaleSpec: Decodable {
    let entries: [SymbolSizeScaleEntrySpec]?
    let scaleType: ScaleTypeSpec?
}

struct LegendConfigSpec: Decodable {
    let visibility: Int32
    let position: Int32
    let alignment: Int32
    let spacing: Double
}

struct PlotStyleSpec: Decodable {
    let kind: Int32
    let colorR: Double
    let colorG: Double
    let colorB: Double
    let colorA: Double
    let width: Double
    let viewPtr: String
    let top: Double
    let right: Double
    let bottom: Double
    let left: Double
}

struct ScrollPositionSpec: Decodable {
    let kind: Int32
    let axis: Int32
    let number: Double
    let time: Int64
}

struct StateRefSpec: Decodable {
    let kind: Int32
    let ptr: String
}

struct DateComponentsSpec: Decodable {
    let year: Int
    let month: Int
    let day: Int
    let hour: Int
    let minute: Int
    let second: Int
}

struct MajorValueAlignmentSpec: Decodable {
    let kind: Int32
    let numberUnit: Double
    let dateUnit: DateComponentsSpec
}

struct ScrollTargetBehaviorSpec: Decodable {
    let kind: Int32
    let xUnit: Double
    let yUnit: Double
    let xDate: DateComponentsSpec
    let yDate: DateComponentsSpec
    let xMajor: MajorValueAlignmentSpec?
    let yMajor: MajorValueAlignmentSpec?
    let limit: Int32
}

struct VisibleDomainLengthSpec: Decodable {
    let kind: Int32
    let number: Double
}

struct MarkDimensionSpec: Decodable {
    let kind: Int32
    let value: Double
}

struct PointPlotDatumSpec: Decodable {
    let x: ValueSpec
    let y: ValueSpec
}

struct RectanglePlotDatumSpec: Decodable {
    let x: ValueSpec?
    let y: ValueSpec?
    let xStart: ValueSpec?
    let xEnd: ValueSpec?
    let yStart: ValueSpec?
    let yEnd: ValueSpec?
    let width: MarkDimensionSpec
    let height: MarkDimensionSpec
}

struct RulePlotDatumSpec: Decodable {
    let x: ValueSpec?
    let y: ValueSpec?
    let xStart: ValueSpec?
    let xEnd: ValueSpec?
    let yStart: ValueSpec?
    let yEnd: ValueSpec?
}

struct ProxyLayerSpec: Decodable {
    let kind: Int32
    let alignment: Int32
    let callbackID: UInt64
    let xState: StateRefSpec?
    let yState: StateRefSpec?
    let range: StateRefSpec?
    let colorR: Double
    let colorG: Double
    let colorB: Double
    let colorA: Double
    let width: Double
    let xFormat: ValueFormatSpec
    let yFormat: ValueFormatSpec
}

struct ProxyGestureSpec: Decodable {
    let kind: Int32
    let state: StateRefSpec?
    let minDistance: Double
}

enum PlottableData {
    case category(String, String)
    case integer(String, Int)
    case number(String, Double)
    case time(String, Date, Calendar.Component)
}

extension ValueSpec {
    func toPlottable() -> PlottableData {
        switch kind {
        case 1:
            return .integer(label, integer)
        case 2:
            return .number(label, number)
        case 3:
            return .time(label, Date(timeIntervalSince1970: Double(timeUnixMS) / 1000), calendarUnit(timeUnit))
        default:
            return .category(label, category)
        }
    }
}
