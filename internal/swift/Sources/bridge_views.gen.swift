// Code generated by applegen; DO NOT EDIT.

import SwiftUI
import AppKit
import Foundation
import WebKit


@_cdecl("SUIText")
public func SUIText(_ s: UnsafePointer<CChar>) -> UnsafeMutableRawPointer {
    let text = String(cString: s)
    let view = AnyView(Text(text))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUITextTimerInterval")
public func SUITextTimerInterval(_ start: Double, _ end: Double, _ pauseTime: Double, _ countsDown: Bool, _ showsHours: Bool) -> UnsafeMutableRawPointer {
    let startDate = Date(timeIntervalSince1970: start)
    let endDate = Date(timeIntervalSince1970: end)
    let pauseDate: Date? = pauseTime == 0 ? nil : Date(timeIntervalSince1970: pauseTime)
    let view = AnyView(Text(timerInterval: startDate...endDate, pauseTime: pauseDate, countsDown: countsDown, showsHours: showsHours))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIImage")
public func SUIImage(_ systemName: UnsafePointer<CChar>) -> UnsafeMutableRawPointer {
    let name = String(cString: systemName)
    let view = AnyView(Image(systemName: name))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIImageNamed")
public func SUIImageNamed(_ name: UnsafePointer<CChar>) -> UnsafeMutableRawPointer {
    let n = String(cString: name)
    let view = AnyView(Image(n))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIImageFromFile")
public func SUIImageFromFile(_ path: UnsafePointer<CChar>) -> UnsafeMutableRawPointer {
    let p = String(cString: path)
    let view: AnyView
    if let nsImage = NSImage(contentsOfFile: p) {
        view = AnyView(Image(nsImage: nsImage))
    } else {
        view = AnyView(Image(systemName: "photo"))
    }
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIAsyncImage")
public func SUIAsyncImage(_ urlStr: UnsafePointer<CChar>) -> UnsafeMutableRawPointer {
    let str = String(cString: urlStr)
    let view: AnyView
    if let url = URL(string: str) {
        view = AnyView(AsyncImage(url: url) { phase in
            switch phase {
            case .success(let image):
                image.resizable().aspectRatio(contentMode: .fit)
            case .failure:
                Image(systemName: "exclamationmark.triangle")
            case .empty:
                ProgressView()
            @unknown default:
                EmptyView()
            }
        })
    } else {
        view = AnyView(Image(systemName: "photo"))
    }
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

private func suiHorizontalAlignment(_ raw: Int32) -> HorizontalAlignment {
    switch raw {
    case 0: return .leading
    case 2: return .trailing
    default: return .center
    }
}

private func suiVerticalAlignment(_ raw: Int32) -> VerticalAlignment {
    switch raw {
    case 0: return .top
    case 2: return .bottom
    case 3: return .firstTextBaseline
    case 4: return .lastTextBaseline
    default: return .center
    }
}

private func suiNavigationSplitViewVisibility(_ raw: Int) -> NavigationSplitViewVisibility {
    switch raw {
    case 1:
        return .all
    case 2:
        return .doubleColumn
    case 3:
        return .detailOnly
    default:
        return .automatic
    }
}

private func suiNavigationSplitViewVisibilityRaw(_ visibility: NavigationSplitViewVisibility) -> Int {
    switch visibility {
    case .all:
        return 1
    case .doubleColumn:
        return 2
    case .detailOnly:
        return 3
    default:
        return 0
    }
}

@_cdecl("SUIVStack")
public func SUIVStack(_ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32) -> UnsafeMutableRawPointer {
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let stack = AnyView(VStack {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(stack)).toOpaque()
}

@_cdecl("SUIVStackSpaced")
public func SUIVStackSpaced(_ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32, _ spacing: Double) -> UnsafeMutableRawPointer {
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let stack = AnyView(VStack(spacing: spacing) {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(stack)).toOpaque()
}

@_cdecl("SUIVStackAligned")
public func SUIVStackAligned(_ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32, _ alignment: Int32) -> UnsafeMutableRawPointer {
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let stack = AnyView(VStack(alignment: suiHorizontalAlignment(alignment)) {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(stack)).toOpaque()
}

@_cdecl("SUIVStackAlignedSpaced")
public func SUIVStackAlignedSpaced(_ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32, _ alignment: Int32, _ spacing: Double) -> UnsafeMutableRawPointer {
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let stack = AnyView(VStack(alignment: suiHorizontalAlignment(alignment), spacing: spacing) {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(stack)).toOpaque()
}

@_cdecl("SUIHStack")
public func SUIHStack(_ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32) -> UnsafeMutableRawPointer {
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let stack = AnyView(HStack {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(stack)).toOpaque()
}

@_cdecl("SUIHStackSpaced")
public func SUIHStackSpaced(_ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32, _ spacing: Double) -> UnsafeMutableRawPointer {
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let stack = AnyView(HStack(spacing: spacing) {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(stack)).toOpaque()
}

@_cdecl("SUIHStackAligned")
public func SUIHStackAligned(_ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32, _ alignment: Int32) -> UnsafeMutableRawPointer {
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let stack = AnyView(HStack(alignment: suiVerticalAlignment(alignment)) {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(stack)).toOpaque()
}

@_cdecl("SUIHStackAlignedSpaced")
public func SUIHStackAlignedSpaced(_ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32, _ alignment: Int32, _ spacing: Double) -> UnsafeMutableRawPointer {
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let stack = AnyView(HStack(alignment: suiVerticalAlignment(alignment), spacing: spacing) {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(stack)).toOpaque()
}

@_cdecl("SUIButton")
public func SUIButton(_ label: UnsafePointer<CChar>, _ callbackID: UInt) -> UnsafeMutableRawPointer {
    let title = String(cString: label)
    let id = callbackID
    let view = AnyView(Button(title) {
        _SUIButtonCallback?(id)
    })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIButtonView")
public func SUIButtonView(_ viewRef: UnsafeMutableRawPointer, _ callbackID: UInt) -> UnsafeMutableRawPointer {
    let base = Unmanaged<Box<AnyView>>.fromOpaque(viewRef).takeUnretainedValue().value
    let id = callbackID
    let view = AnyView(Button(action: {
        _SUIButtonCallback?(id)
    }) {
        base
    })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIButtonWithImage")
public func SUIButtonWithImage(_ systemName: UnsafePointer<CChar>, _ callbackID: UInt) -> UnsafeMutableRawPointer {
    let name = String(cString: systemName)
    let id = callbackID
    let view = AnyView(Button(action: {
        _SUIButtonCallback?(id)
    }) {
        Image(systemName: name)
    })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIButtonWithLabel")
public func SUIButtonWithLabel(_ text: UnsafePointer<CChar>, _ systemImage: UnsafePointer<CChar>, _ callbackID: UInt) -> UnsafeMutableRawPointer {
    let t = String(cString: text)
    let img = String(cString: systemImage)
    let id = callbackID
    let view = AnyView(Button(action: {
        _SUIButtonCallback?(id)
    }) {
        Label(t, systemImage: img)
    })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIPasteButton")
@MainActor
public func SUIPasteButton(_ labelPtr: UnsafePointer<CChar>, _ stateRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let title = String(cString: labelPtr)
    let state = Unmanaged<BridgedStringState>.fromOpaque(stateRef).takeUnretainedValue()
    let view = AnyView(Button(title) {
        if let pasted = NSPasteboard.general.string(forType: .string), !pasted.isEmpty {
            state.setAndBump(pasted)
        }
    })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIShareLinkURL")
public func SUIShareLinkURL(_ titlePtr: UnsafePointer<CChar>, _ urlPtr: UnsafePointer<CChar>) -> UnsafeMutableRawPointer {
    let title = String(cString: titlePtr)
    let urlString = String(cString: urlPtr)
    let view: AnyView
    if let url = URL(string: urlString) {
        view = AnyView(ShareLink(item: url) {
            Text(title)
        })
    } else {
        view = AnyView(Button(title) {})
    }
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIShareLinkItem")
public func SUIShareLinkItem(_ titlePtr: UnsafePointer<CChar>, _ kindPtr: UnsafePointer<CChar>, _ valuePtr: UnsafePointer<CChar>, _ filePathPtr: UnsafePointer<CChar>) -> UnsafeMutableRawPointer {
    let title = String(cString: titlePtr)
    let kind = String(cString: kindPtr)
    let value = String(cString: valuePtr)
    let filePath = String(cString: filePathPtr)
    let view: AnyView
    switch kind {
    case "text":
        view = AnyView(ShareLink(item: value) {
            Text(title)
        })
    case "url":
        if let url = URL(string: value) {
            view = AnyView(ShareLink(item: url) {
                Text(title)
            })
        } else {
            view = AnyView(Button(title) {})
        }
    case "file":
        let path = filePath.isEmpty ? value : filePath
        if path.isEmpty {
            view = AnyView(Button(title) {})
        } else {
            view = AnyView(ShareLink(item: URL(fileURLWithPath: path)) {
                Text(title)
            })
        }
    default:
        view = AnyView(Button(title) {})
    }
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUISpacer")
public func SUISpacer() -> UnsafeMutableRawPointer {
    let view = AnyView(Spacer())
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIDivider")
public func SUIDivider() -> UnsafeMutableRawPointer {
    let view = AnyView(Divider())
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUILabel")
public func SUILabel(_ text: UnsafePointer<CChar>, _ systemImage: UnsafePointer<CChar>) -> UnsafeMutableRawPointer {
    let t = String(cString: text)
    let img = String(cString: systemImage)
    let view = AnyView(Label(t, systemImage: img))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIProgressViewSpinning")
public func SUIProgressViewSpinning() -> UnsafeMutableRawPointer {
    let view = AnyView(ProgressView())
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIProgressViewLinear")
public func SUIProgressViewLinear(_ value: Double, _ total: Double) -> UnsafeMutableRawPointer {
    let view = AnyView(ProgressView(value: value, total: total))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIGroupBox")
public func SUIGroupBox(_ labelPtr: UnsafePointer<CChar>, _ contentRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let label = String(cString: labelPtr)
    let content = Unmanaged<Box<AnyView>>.fromOpaque(contentRef).takeUnretainedValue().value
    let view = AnyView(GroupBox(label) { content })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIEmptyView")
public func SUIEmptyView() -> UnsafeMutableRawPointer {
    let view = AnyView(EmptyView())
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIScrollView")
public func SUIScrollView(_ contentRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let content = Unmanaged<Box<AnyView>>.fromOpaque(contentRef).takeUnretainedValue().value
    let view = AnyView(ScrollView { content })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIScrollViewReader")
@MainActor
public func SUIScrollViewReader(_ positionRef: UnsafeMutableRawPointer, _ anchor: Int32, _ contentRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let position = Unmanaged<BridgedIntState>.fromOpaque(positionRef).takeUnretainedValue()
    let content = Unmanaged<Box<AnyView>>.fromOpaque(contentRef).takeUnretainedValue().value
    let view = AnyView(BridgedScrollViewReaderView(position: position, anchor: suiScrollAnchorUnitPoint(anchor), content: content))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedScrollViewReaderView: View {
    var position: BridgedIntState
    let anchor: UnitPoint
    let content: AnyView

    var body: some View {
        ScrollViewReader { proxy in
            content
                .onAppear {
                    if position.value != 0 {
                        proxy.scrollTo(position.value, anchor: anchor)
                    }
                }
                .onChange(of: position.value) { _, newValue in
                    if newValue != 0 {
                        proxy.scrollTo(newValue, anchor: anchor)
                    }
                }
        }
    }
}

// MARK: - Additional containers

@_cdecl("SUIZStack")
public func SUIZStack(_ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32) -> UnsafeMutableRawPointer {
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let stack = AnyView(ZStack {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(stack)).toOpaque()
}

@_cdecl("SUILazyVStack")
public func SUILazyVStack(_ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32) -> UnsafeMutableRawPointer {
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let stack = AnyView(LazyVStack {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(stack)).toOpaque()
}

@_cdecl("SUILazyHStack")
public func SUILazyHStack(_ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32) -> UnsafeMutableRawPointer {
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let stack = AnyView(LazyHStack {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(stack)).toOpaque()
}

@_cdecl("SUIList")
@MainActor
public func SUIList(_ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32) -> UnsafeMutableRawPointer {
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let list = AnyView(List {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(list)).toOpaque()
}

@_cdecl("SUISelectableList")
@MainActor
public func SUISelectableList(_ selectionRef: UnsafeMutableRawPointer, _ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32) -> UnsafeMutableRawPointer {
    let state = Unmanaged<BridgedIntState>.fromOpaque(selectionRef).takeUnretainedValue()
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let list = AnyView(List(selection: Binding<Int?>(
        get: { state.value == 0 ? nil : state.value },
        set: { state.setAndBump($0 ?? 0) }
    )) {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(list)).toOpaque()
}

@_cdecl("SUIForm")
public func SUIForm(_ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32) -> UnsafeMutableRawPointer {
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let form = AnyView(Form {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(form)).toOpaque()
}

@_cdecl("SUISection")
public func SUISection(_ headerPtr: UnsafePointer<CChar>, _ contentRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let header = String(cString: headerPtr)
    let content = Unmanaged<Box<AnyView>>.fromOpaque(contentRef).takeUnretainedValue().value
    let view = AnyView(Section(header) { content })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUISectionExpanded")
@MainActor
public func SUISectionExpanded(_ headerPtr: UnsafePointer<CChar>, _ expandedRef: UnsafeMutableRawPointer, _ contentRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let header = String(cString: headerPtr)
    let expanded = Unmanaged<BridgedBoolState>.fromOpaque(expandedRef).takeUnretainedValue()
    let content = Unmanaged<Box<AnyView>>.fromOpaque(contentRef).takeUnretainedValue().value
    let view = AnyView(Section(header, isExpanded: Binding(
        get: { expanded.value },
        set: { expanded.value = $0 }
    )) {
        content
    })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUITabView")
public func SUITabView(_ children: UnsafePointer<UnsafeMutableRawPointer>, _ count: Int32) -> UnsafeMutableRawPointer {
    let views = (0..<Int(count)).map { i in
        Unmanaged<Box<AnyView>>.fromOpaque(children[i]).takeUnretainedValue().value
    }
    let tab = AnyView(TabView {
        ForEach(views.indices, id: \.self) { i in
            views[i]
        }
    })
    return Unmanaged.passRetained(Box(tab)).toOpaque()
}

@_cdecl("SUINavigationLink")
public func SUINavigationLink(_ labelPtr: UnsafePointer<CChar>, _ destRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let label = String(cString: labelPtr)
    let dest = Unmanaged<Box<AnyView>>.fromOpaque(destRef).takeUnretainedValue().value
    let view = AnyView(NavigationLink(label) { dest })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUINavigationStack")
@MainActor
public func SUINavigationStack(_ contentRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let content = Unmanaged<Box<AnyView>>.fromOpaque(contentRef).takeUnretainedValue().value
    let view = AnyView(NavigationStack { content })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUINavigationSplitView")
@MainActor
public func SUINavigationSplitView(_ sidebarRef: UnsafeMutableRawPointer, _ detailRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let sidebar = Unmanaged<Box<AnyView>>.fromOpaque(sidebarRef).takeUnretainedValue().value
    let detail = Unmanaged<Box<AnyView>>.fromOpaque(detailRef).takeUnretainedValue().value
    let view = AnyView(NavigationSplitView {
        sidebar
    } detail: {
        detail
    })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUINavigationSplitViewTriple")
@MainActor
public func SUINavigationSplitViewTriple(_ sidebarRef: UnsafeMutableRawPointer, _ contentRef: UnsafeMutableRawPointer, _ detailRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let sidebar = Unmanaged<Box<AnyView>>.fromOpaque(sidebarRef).takeUnretainedValue().value
    let content = Unmanaged<Box<AnyView>>.fromOpaque(contentRef).takeUnretainedValue().value
    let detail = Unmanaged<Box<AnyView>>.fromOpaque(detailRef).takeUnretainedValue().value
    let view = AnyView(NavigationSplitView {
        sidebar
    } content: {
        content
    } detail: {
        detail
    })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUINavigationSplitViewVisibility")
@MainActor
public func SUINavigationSplitViewVisibility(_ visibilityRef: UnsafeMutableRawPointer, _ sidebarRef: UnsafeMutableRawPointer, _ detailRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let visibility = Unmanaged<BridgedIntState>.fromOpaque(visibilityRef).takeUnretainedValue()
    let sidebar = Unmanaged<Box<AnyView>>.fromOpaque(sidebarRef).takeUnretainedValue().value
    let detail = Unmanaged<Box<AnyView>>.fromOpaque(detailRef).takeUnretainedValue().value
    let view = AnyView(NavigationSplitView(columnVisibility: Binding(
        get: { suiNavigationSplitViewVisibility(visibility.value) },
        set: { visibility.value = suiNavigationSplitViewVisibilityRaw($0) }
    )) {
        sidebar
    } detail: {
        detail
    })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUINavigationSplitViewTripleVisibility")
@MainActor
public func SUINavigationSplitViewTripleVisibility(_ visibilityRef: UnsafeMutableRawPointer, _ sidebarRef: UnsafeMutableRawPointer, _ contentRef: UnsafeMutableRawPointer, _ detailRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let visibility = Unmanaged<BridgedIntState>.fromOpaque(visibilityRef).takeUnretainedValue()
    let sidebar = Unmanaged<Box<AnyView>>.fromOpaque(sidebarRef).takeUnretainedValue().value
    let content = Unmanaged<Box<AnyView>>.fromOpaque(contentRef).takeUnretainedValue().value
    let detail = Unmanaged<Box<AnyView>>.fromOpaque(detailRef).takeUnretainedValue().value
    let view = AnyView(NavigationSplitView(columnVisibility: Binding(
        get: { suiNavigationSplitViewVisibility(visibility.value) },
        set: { visibility.value = suiNavigationSplitViewVisibilityRaw($0) }
    )) {
        sidebar
    } content: {
        content
    } detail: {
        detail
    })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIMenu")
public func SUIMenu(_ labelPtr: UnsafePointer<CChar>, _ contentRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let label = String(cString: labelPtr)
    let content = Unmanaged<Box<AnyView>>.fromOpaque(contentRef).takeUnretainedValue().value
    let view = AnyView(Menu(label) { content })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIMenuView")
public func SUIMenuView(_ labelRef: UnsafeMutableRawPointer, _ contentRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let label = Unmanaged<Box<AnyView>>.fromOpaque(labelRef).takeUnretainedValue().value
    let content = Unmanaged<Box<AnyView>>.fromOpaque(contentRef).takeUnretainedValue().value
    let view = AnyView(Menu {
        content
    } label: {
        label
    })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIDisclosureGroupView")
@MainActor
public func SUIDisclosureGroupView(_ labelRef: UnsafeMutableRawPointer, _ expandedRef: UnsafeMutableRawPointer, _ contentRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let label = Unmanaged<Box<AnyView>>.fromOpaque(labelRef).takeUnretainedValue().value
    let expanded = Unmanaged<BridgedBoolState>.fromOpaque(expandedRef).takeUnretainedValue()
    let content = Unmanaged<Box<AnyView>>.fromOpaque(contentRef).takeUnretainedValue().value
    let view = AnyView(DisclosureGroup(isExpanded: suiBoolBinding(expanded)) {
        content
    } label: {
        label
    })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUISectionExpandedView")
@MainActor
public func SUISectionExpandedView(_ headerRef: UnsafeMutableRawPointer, _ expandedRef: UnsafeMutableRawPointer, _ contentRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let header = Unmanaged<Box<AnyView>>.fromOpaque(headerRef).takeUnretainedValue().value
    let expanded = Unmanaged<BridgedBoolState>.fromOpaque(expandedRef).takeUnretainedValue()
    let content = Unmanaged<Box<AnyView>>.fromOpaque(contentRef).takeUnretainedValue().value
    let view = AnyView(Section(isExpanded: suiBoolBinding(expanded)) {
        content
    } header: {
        header
    })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedPhaseAnimatorView: View {
    let phases: [Int32]
    let trigger: Int32?
    let callbackID: UInt
    let animationKind: Int32

    var body: some View {
        let content: (Int32) -> AnyView = { phase in
            if let ptr = _SUIViewBuilderCallback?(callbackID, Int(phase)) {
                return Unmanaged<Box<AnyView>>.fromOpaque(ptr).takeRetainedValue().value
            }
            return AnyView(EmptyView())
        }
        if let trigger {
            PhaseAnimator(phases, trigger: trigger) { phase in
                content(phase)
            } animation: { _ in
                Optional(animationForKind(animationKind))
            }
        } else {
            PhaseAnimator(phases) { phase in
                content(phase)
            } animation: { _ in
                Optional(animationForKind(animationKind))
            }
        }
    }
}

@_cdecl("SUIPhaseAnimator")
@MainActor
public func SUIPhaseAnimator(_ phasesRef: UnsafePointer<Int32>, _ count: Int32, _ callbackID: UInt, _ animationKind: Int32) -> UnsafeMutableRawPointer {
    let phases = Array(UnsafeBufferPointer(start: phasesRef, count: Int(count)))
    let view = AnyView(BridgedPhaseAnimatorView(phases: phases, trigger: nil, callbackID: callbackID, animationKind: animationKind))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIPhaseAnimatorTriggered")
@MainActor
public func SUIPhaseAnimatorTriggered(_ phasesRef: UnsafePointer<Int32>, _ count: Int32, _ trigger: Int32, _ callbackID: UInt, _ animationKind: Int32) -> UnsafeMutableRawPointer {
    let phases = Array(UnsafeBufferPointer(start: phasesRef, count: Int(count)))
    let view = AnyView(BridgedPhaseAnimatorView(phases: phases, trigger: trigger, callbackID: callbackID, animationKind: animationKind))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedSliderTickContentForEachView: View {
    let values: [Int32]
    let callbackID: UInt

    var body: some View {
        ForEach(values, id: \.self) { value in
            if let ptr = _SUIViewBuilderCallback?(callbackID, Int(value)) {
                Unmanaged<Box<AnyView>>.fromOpaque(ptr).takeRetainedValue().value
            } else {
                AnyView(EmptyView())
            }
        }
    }
}

@_cdecl("SUISliderTickContentForEach")
@MainActor
public func SUISliderTickContentForEach(_ valuesRef: UnsafePointer<Int32>, _ count: Int32, _ id: Int32, _ callbackID: UInt) -> UnsafeMutableRawPointer {
    let values = Array(UnsafeBufferPointer(start: valuesRef, count: Int(count)))
    let view = AnyView(BridgedSliderTickContentForEachView(values: values, callbackID: callbackID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

// MARK: - Shapes and display views

@_cdecl("SUIColor")
public func SUIColor(_ r: Double, _ g: Double, _ b: Double, _ a: Double) -> UnsafeMutableRawPointer {
    let view = AnyView(Color(red: r, green: g, blue: b, opacity: a))
    return retainView(view)
}

@_cdecl("SUILinearGradient")
public func SUILinearGradient(
    _ startR: Double, _ startG: Double, _ startB: Double, _ startA: Double,
    _ endR: Double, _ endG: Double, _ endB: Double, _ endA: Double,
    _ startX: Double, _ startY: Double,
    _ endX: Double, _ endY: Double
) -> UnsafeMutableRawPointer {
    let start = Color(red: startR, green: startG, blue: startB, opacity: startA)
    let end = Color(red: endR, green: endG, blue: endB, opacity: endA)
    let view = AnyView(LinearGradient(
        colors: [start, end],
        startPoint: UnitPoint(x: startX, y: startY),
        endPoint: UnitPoint(x: endX, y: endY)
    ))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIRadialGradient")
public func SUIRadialGradient(
    _ innerR: Double, _ innerG: Double, _ innerB: Double, _ innerA: Double,
    _ outerR: Double, _ outerG: Double, _ outerB: Double, _ outerA: Double,
    _ centerX: Double, _ centerY: Double,
    _ startRadius: Double, _ endRadius: Double
) -> UnsafeMutableRawPointer {
    let inner = Color(red: innerR, green: innerG, blue: innerB, opacity: innerA)
    let outer = Color(red: outerR, green: outerG, blue: outerB, opacity: outerA)
    let view = AnyView(RadialGradient(
        colors: [inner, outer],
        center: UnitPoint(x: centerX, y: centerY),
        startRadius: startRadius,
        endRadius: endRadius
    ))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIMeshGradient4")
public func SUIMeshGradient4(_ dataPtr: UnsafePointer<CChar>) -> UnsafeMutableRawPointer {
    let values = String(cString: dataPtr)
        .split(separator: ",")
        .compactMap { Double($0.trimmingCharacters(in: .whitespacesAndNewlines)) }
    guard values.count == 16 else {
        let view = AnyView(LinearGradient(colors: [.clear, .clear], startPoint: .topLeading, endPoint: .bottomTrailing))
        return Unmanaged.passRetained(Box(view)).toOpaque()
    }
    let topLeading = Color(red: values[0], green: values[1], blue: values[2], opacity: values[3])
    let topTrailing = Color(red: values[4], green: values[5], blue: values[6], opacity: values[7])
    let bottomLeading = Color(red: values[8], green: values[9], blue: values[10], opacity: values[11])
    let bottomTrailing = Color(red: values[12], green: values[13], blue: values[14], opacity: values[15])
    let view: AnyView
    if #available(macOS 15.0, *) {
        view = AnyView(MeshGradient(
            width: 2,
            height: 2,
            points: [
                SIMD2<Float>(0, 0), SIMD2<Float>(1, 0),
                SIMD2<Float>(0, 1), SIMD2<Float>(1, 1),
            ],
            colors: [topLeading, topTrailing, bottomLeading, bottomTrailing]
        ))
    } else {
        view = AnyView(LinearGradient(
            colors: [topLeading, bottomTrailing],
            startPoint: .topLeading,
            endPoint: .bottomTrailing
        ))
    }
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUICircle")
public func SUICircle() -> UnsafeMutableRawPointer {
    let shape = Circle()
    return retainView(AnyView(shape), shape: AnyShape(shape))
}

@_cdecl("SUIRectangle")
public func SUIRectangle() -> UnsafeMutableRawPointer {
    let shape = Rectangle()
    return retainView(AnyView(shape), shape: AnyShape(shape))
}

@_cdecl("SUIRoundedRectangle")
public func SUIRoundedRectangle(_ cornerRadius: Double) -> UnsafeMutableRawPointer {
    let shape = RoundedRectangle(cornerRadius: cornerRadius)
    return retainView(AnyView(shape), shape: AnyShape(shape))
}

@_cdecl("SUICapsule")
public func SUICapsule() -> UnsafeMutableRawPointer {
    let shape = Capsule()
    return retainView(AnyView(shape), shape: AnyShape(shape))
}

// MARK: - Path / Canvas opcode decoder (v1)
//
// Wire format is shared with the Go side (canvas.go). All integers are
// little-endian; floats are IEEE-754 f64. Path opcodes live in 0x01..0x0F.
// Canvas opcodes live in 0x10..0x1F. See canvas.go for the authoritative
// opcode table.

private let suiPathOpMove: UInt8 = 0x01
private let suiPathOpLine: UInt8 = 0x02
private let suiPathOpQuad: UInt8 = 0x03
private let suiPathOpCubic: UInt8 = 0x04
private let suiPathOpArc: UInt8 = 0x05
private let suiPathOpRect: UInt8 = 0x06
private let suiPathOpEllipse: UInt8 = 0x07
private let suiPathOpClose: UInt8 = 0x08

private let suiCanvasOpStroke: UInt8 = 0x10
private let suiCanvasOpFill: UInt8 = 0x11
private let suiCanvasOpClip: UInt8 = 0x12
private let suiCanvasOpTranslate: UInt8 = 0x13
private let suiCanvasOpScale: UInt8 = 0x14
private let suiCanvasOpRotate: UInt8 = 0x15
private let suiCanvasOpSave: UInt8 = 0x16
private let suiCanvasOpRestore: UInt8 = 0x17
private let suiCanvasOpOpacity: UInt8 = 0x18

// suiDecodePath consumes len opcode-bytes starting at offset and builds a
// SwiftUI Path. On decode error (truncated operand or unknown opcode) it
// returns the partial path built so far — callers treat this as best-effort.
private func suiDecodePath(_ reader: inout SUIModifierReader, _ length: Int) -> Path {
    var path = Path()
    let end = reader.offset + length
    while reader.offset < end {
        guard let op = reader.readU8() else { break }
        switch op {
        case suiPathOpMove:
            guard let x = reader.readF64(), let y = reader.readF64() else { return path }
            path.move(to: CGPoint(x: x, y: y))
        case suiPathOpLine:
            guard let x = reader.readF64(), let y = reader.readF64() else { return path }
            path.addLine(to: CGPoint(x: x, y: y))
        case suiPathOpQuad:
            guard let cx = reader.readF64(), let cy = reader.readF64(),
                  let x = reader.readF64(), let y = reader.readF64() else { return path }
            path.addQuadCurve(to: CGPoint(x: x, y: y), control: CGPoint(x: cx, y: cy))
        case suiPathOpCubic:
            guard let c1x = reader.readF64(), let c1y = reader.readF64(),
                  let c2x = reader.readF64(), let c2y = reader.readF64(),
                  let x = reader.readF64(), let y = reader.readF64() else { return path }
            path.addCurve(to: CGPoint(x: x, y: y),
                          control1: CGPoint(x: c1x, y: c1y),
                          control2: CGPoint(x: c2x, y: c2y))
        case suiPathOpArc:
            guard let cx = reader.readF64(), let cy = reader.readF64(),
                  let r = reader.readF64(), let a0 = reader.readF64(),
                  let a1 = reader.readF64(), let cw = reader.readU8() else { return path }
            path.addArc(center: CGPoint(x: cx, y: cy), radius: r,
                        startAngle: .radians(a0), endAngle: .radians(a1),
                        clockwise: cw != 0)
        case suiPathOpRect:
            guard let x = reader.readF64(), let y = reader.readF64(),
                  let w = reader.readF64(), let h = reader.readF64() else { return path }
            path.addRect(CGRect(x: x, y: y, width: w, height: h))
        case suiPathOpEllipse:
            guard let x = reader.readF64(), let y = reader.readF64(),
                  let w = reader.readF64(), let h = reader.readF64() else { return path }
            path.addEllipse(in: CGRect(x: x, y: y, width: w, height: h))
        case suiPathOpClose:
            path.closeSubpath()
        default:
            // Unknown opcode: stop decoding to avoid misreading operands.
            return path
        }
    }
    return path
}

@_cdecl("SUIPath")
public func SUIPath(_ bufPtr: UnsafePointer<UInt8>?, _ bufLen: Int32) -> UnsafeMutableRawPointer {
    var reader = SUIModifierReader(bufPtr, Int(bufLen))
    let path = suiDecodePath(&reader, Int(bufLen))
    return retainView(AnyView(path), shape: AnyShape(path))
}

// MARK: - Canvas state + renderer

@Observable
final class BridgedCanvasState: AnyBridgedState, @unchecked Sendable {
    var blob: Data
    private(set) var gen: UInt32 = 0
    init(_ blob: Data) { self.blob = blob }

    func setAndBump(_ v: Data) {
        self.blob = v
        self.gen &+= 1
    }
}

@_cdecl("SUICanvasStateNew")
public func SUICanvasStateNew(_ bufPtr: UnsafePointer<UInt8>?, _ bufLen: Int32) -> UnsafeMutableRawPointer {
    let data: Data
    if let bufPtr, bufLen > 0 {
        data = Data(bytes: bufPtr, count: Int(bufLen))
    } else {
        data = Data()
    }
    return Unmanaged.passRetained(BridgedCanvasState(data)).toOpaque()
}

@_cdecl("SUICanvasStateSet")
public func SUICanvasStateSet(_ ref: UnsafeMutableRawPointer, _ bufPtr: UnsafePointer<UInt8>?, _ bufLen: Int32) {
    let s = Unmanaged<BridgedCanvasState>.fromOpaque(ref).takeUnretainedValue()
    let data: Data
    if let bufPtr, bufLen > 0 {
        data = Data(bytes: bufPtr, count: Int(bufLen))
    } else {
        data = Data()
    }
    DispatchQueue.main.async { s.setAndBump(data) }
}

@MainActor
private struct BridgedCanvasView: View {
    var state: BridgedCanvasState
    let width: Double
    let height: Double

    var body: some View {
        Canvas { ctx, _ in
            BridgedCanvasView.render(ctx: &ctx, blob: state.blob)
        }
        .frame(width: width, height: height)
    }

    static func render(ctx: inout GraphicsContext, blob: Data) {
        blob.withUnsafeBytes { raw in
            let base = raw.baseAddress?.assumingMemoryBound(to: UInt8.self)
            var reader = SUIModifierReader(base, blob.count)
            while reader.remaining > 0 {
                guard let op = reader.readU8() else { return }
                switch op {
                case suiCanvasOpStroke:
                    guard let pathLen = reader.readU32(),
                          let r = reader.readF64(), let g = reader.readF64(),
                          let b = reader.readF64(), let a = reader.readF64(),
                          let w = reader.readF64(),
                          let dashN = reader.readU8() else { return }
                    var dashes: [CGFloat] = []
                    if dashN > 0 {
                        dashes.reserveCapacity(Int(dashN))
                        for _ in 0..<dashN {
                            guard let d = reader.readF64() else { return }
                            dashes.append(CGFloat(d))
                        }
                    }
                    let path = suiDecodePath(&reader, Int(pathLen))
                    let color = Color(red: r, green: g, blue: b, opacity: a)
                    let style = StrokeStyle(lineWidth: w, dash: dashes)
                    ctx.stroke(path, with: .color(color), style: style)
                case suiCanvasOpFill:
                    guard let pathLen = reader.readU32(),
                          let r = reader.readF64(), let g = reader.readF64(),
                          let b = reader.readF64(), let a = reader.readF64() else { return }
                    let path = suiDecodePath(&reader, Int(pathLen))
                    ctx.fill(path, with: .color(Color(red: r, green: g, blue: b, opacity: a)))
                case suiCanvasOpClip:
                    guard let pathLen = reader.readU32() else { return }
                    let path = suiDecodePath(&reader, Int(pathLen))
                    ctx.clip(to: path)
                case suiCanvasOpTranslate:
                    guard let dx = reader.readF64(), let dy = reader.readF64() else { return }
                    ctx.translateBy(x: dx, y: dy)
                case suiCanvasOpScale:
                    guard let sx = reader.readF64(), let sy = reader.readF64() else { return }
                    ctx.scaleBy(x: sx, y: sy)
                case suiCanvasOpRotate:
                    guard let rad = reader.readF64() else { return }
                    ctx.rotate(by: .radians(rad))
                case suiCanvasOpSave, suiCanvasOpRestore:
                    // GraphicsContext is a value type; save/restore scoping is
                    // expressed by drawLayer { ... } in native Swift. In v1 the
                    // opcode is reserved but the decoder treats it as a no-op.
                    // v2 may introduce a begin/end-scope pair that actually
                    // branches into a nested reader. For now, callers emitting
                    // transforms should avoid relying on scope semantics —
                    // transforms accumulate linearly until reset with an
                    // inverse translate/scale/rotate.
                    break
                case suiCanvasOpOpacity:
                    guard let a = reader.readF64() else { return }
                    ctx.opacity = a
                default:
                    return
                }
            }
        }
    }
}

@_cdecl("SUICanvas")
public func SUICanvas(_ stateRef: UnsafeMutableRawPointer, _ width: Double, _ height: Double) -> UnsafeMutableRawPointer {
    let state = Unmanaged<BridgedCanvasState>.fromOpaque(stateRef).takeUnretainedValue()
    // BridgedCanvasView.body is @MainActor (struct is @MainActor); construction
    // itself is just initializer assignment and is safe off-main-thread. The
    // view's render closure only fires when SwiftUI lays out the Canvas, which
    // always happens on the main actor.
    let view = AnyView(BridgedCanvasView(state: state, width: width, height: height))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUILink")
public func SUILink(_ titlePtr: UnsafePointer<CChar>, _ urlPtr: UnsafePointer<CChar>) -> UnsafeMutableRawPointer {
    let title = String(cString: titlePtr)
    let urlString = String(cString: urlPtr)
    let view: AnyView
    if let url = URL(string: urlString) {
        view = AnyView(Link(title, destination: url))
    } else {
        view = AnyView(Text(title))
    }
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIGauge")
public func SUIGauge(_ value: Double, _ labelPtr: UnsafePointer<CChar>) -> UnsafeMutableRawPointer {
    let label = String(cString: labelPtr)
    let view = AnyView(Gauge(value: value) { Text(label) })
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@_cdecl("SUIWebPageCreate")
@MainActor
public func SUIWebPageCreate() -> UnsafeMutableRawPointer {
    let page = WebPage()
    return Unmanaged.passRetained(page).toOpaque()
}

@_cdecl("SUIWebPageLoadURL")
@MainActor
public func SUIWebPageLoadURL(_ pageRef: UnsafeMutableRawPointer, _ urlPtr: UnsafePointer<CChar>) {
    let page = Unmanaged<WebPage>.fromOpaque(pageRef).takeUnretainedValue()
    let urlString = String(cString: urlPtr)
    guard let url = URL(string: urlString) else {
        return
    }
    page.load(URLRequest(url: url))
}

@_cdecl("SUIWebPageReload")
@MainActor
public func SUIWebPageReload(_ pageRef: UnsafeMutableRawPointer) {
    let page = Unmanaged<WebPage>.fromOpaque(pageRef).takeUnretainedValue()
    page.reload()
}

@_cdecl("SUIWebViewPage")
@MainActor
public func SUIWebViewPage(_ pageRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let page = Unmanaged<WebPage>.fromOpaque(pageRef).takeUnretainedValue()
    let view = AnyView(WebView(page))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

// MARK: - Binding views

@_cdecl("SUIToggle")
@MainActor
public func SUIToggle(_ labelPtr: UnsafePointer<CChar>, _ stateRef: UnsafeMutableRawPointer, _ callbackID: UInt) -> UnsafeMutableRawPointer {
    let label = String(cString: labelPtr)
    let state = Unmanaged<BridgedIntState>.fromOpaque(stateRef).takeUnretainedValue()
    let view = AnyView(BridgedToggle(label: label, state: state, callbackID: callbackID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedToggle: View {
    let label: String
    var state: BridgedIntState
    let callbackID: UInt

    var body: some View {
        Toggle(label, isOn: Binding(
            get: { state.value != 0 },
            set: { newValue in
                state.setAndBump(newValue ? 1 : 0)
                _SUIButtonCallback?(callbackID)
            }
        ))
    }
}

// Environment keys backing the SUIViewTextFieldStyle and
// SUIViewScrollContentBackgroundHidden modifiers. The AppKit-backed editable
// controls read these to mirror the requested SwiftUI styling.
private struct SUITextFieldStyleKindKey: EnvironmentKey {
    static let defaultValue: Int32 = 0
}

private struct SUIHiddenScrollContentBackgroundKey: EnvironmentKey {
    static let defaultValue = false
}

extension EnvironmentValues {
    var suiTextFieldStyleKind: Int32 {
        get { self[SUITextFieldStyleKindKey.self] }
        set { self[SUITextFieldStyleKindKey.self] = newValue }
    }

    var suiScrollContentBackgroundHidden: Bool {
        get { self[SUIHiddenScrollContentBackgroundKey.self] }
        set { self[SUIHiddenScrollContentBackgroundKey.self] = newValue }
    }
}



@_cdecl("SUITextField")
@MainActor
public func SUITextField(_ placeholderPtr: UnsafePointer<CChar>, _ stateRef: UnsafeMutableRawPointer, _ onSubmitID: UInt) -> UnsafeMutableRawPointer {
    let placeholder = String(cString: placeholderPtr)
    let state = Unmanaged<BridgedStringState>.fromOpaque(stateRef).takeUnretainedValue()
    let view = AnyView(BridgedTextField(placeholder: placeholder, state: state, onSubmitID: onSubmitID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedTextField: View {
    let placeholder: String
    let state: BridgedStringState
    let onSubmitID: UInt

    var body: some View {
        @Bindable var bindableState = state
        SUIEditableTextField(
            placeholder: placeholder,
            text: $bindableState.value,
            selectionStart: nil,
            selectionEnd: nil,
            onChange: nil,
            onSubmit: {
                if onSubmitID != 0 {
                    _SUIButtonCallback?(onSubmitID)
                }
            }
        )
    }
}

@_cdecl("SUITextFieldCallbacks")
@MainActor
public func SUITextFieldCallbacks(_ placeholderPtr: UnsafePointer<CChar>, _ stateRef: UnsafeMutableRawPointer, _ onChangeID: UInt, _ onSubmitID: UInt) -> UnsafeMutableRawPointer {
    let placeholder = String(cString: placeholderPtr)
    let state = Unmanaged<BridgedStringState>.fromOpaque(stateRef).takeUnretainedValue()
    let view = AnyView(BridgedTextFieldCallbacks(placeholder: placeholder, state: state, onChangeID: onChangeID, onSubmitID: onSubmitID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedTextFieldCallbacks: View {
    let placeholder: String
    let state: BridgedStringState
    let onChangeID: UInt
    let onSubmitID: UInt

    var body: some View {
        @Bindable var bindableState = state
        SUIEditableTextField(
            placeholder: placeholder,
            text: $bindableState.value,
            selectionStart: nil,
            selectionEnd: nil,
            onChange: {
                if onChangeID != 0 {
                    _SUIButtonCallback?(onChangeID)
                }
            },
            onSubmit: {
                if onSubmitID != 0 {
                    _SUIButtonCallback?(onSubmitID)
                }
            }
        )
    }
}

@_cdecl("SUITextFieldSelection")
@MainActor
public func SUITextFieldSelection(_ placeholderPtr: UnsafePointer<CChar>, _ stateRef: UnsafeMutableRawPointer, _ selectionRef: UnsafeMutableRawPointer, _ onSubmitID: UInt) -> UnsafeMutableRawPointer {
    let placeholder = String(cString: placeholderPtr)
    let state = Unmanaged<BridgedStringState>.fromOpaque(stateRef).takeUnretainedValue()
    let selection = Unmanaged<BridgedTextSelectionState>.fromOpaque(selectionRef).takeUnretainedValue()
    let view = AnyView(BridgedTextFieldSelection(placeholder: placeholder, state: state, selection: selection, onSubmitID: onSubmitID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedTextFieldSelection: View {
    let placeholder: String
    let state: BridgedStringState
    let selection: BridgedTextSelectionState
    let onSubmitID: UInt

    var body: some View {
        @Bindable var bindableState = state
        @Bindable var bindableSelection = selection
        SUIEditableTextField(
            placeholder: placeholder,
            text: $bindableState.value,
            selectionStart: $bindableSelection.start,
            selectionEnd: $bindableSelection.end,
            onChange: nil,
            onSubmit: {
                if onSubmitID != 0 {
                    _SUIButtonCallback?(onSubmitID)
                }
            }
        )
    }
}

@_cdecl("SUITextFieldCallbacksSelection")
@MainActor
public func SUITextFieldCallbacksSelection(_ placeholderPtr: UnsafePointer<CChar>, _ stateRef: UnsafeMutableRawPointer, _ selectionRef: UnsafeMutableRawPointer, _ onChangeID: UInt, _ onSubmitID: UInt) -> UnsafeMutableRawPointer {
    let placeholder = String(cString: placeholderPtr)
    let state = Unmanaged<BridgedStringState>.fromOpaque(stateRef).takeUnretainedValue()
    let selection = Unmanaged<BridgedTextSelectionState>.fromOpaque(selectionRef).takeUnretainedValue()
    let view = AnyView(BridgedTextFieldCallbacksSelection(placeholder: placeholder, state: state, selection: selection, onChangeID: onChangeID, onSubmitID: onSubmitID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedTextFieldCallbacksSelection: View {
    let placeholder: String
    let state: BridgedStringState
    let selection: BridgedTextSelectionState
    let onChangeID: UInt
    let onSubmitID: UInt

    var body: some View {
        @Bindable var bindableState = state
        @Bindable var bindableSelection = selection
        SUIEditableTextField(
            placeholder: placeholder,
            text: $bindableState.value,
            selectionStart: $bindableSelection.start,
            selectionEnd: $bindableSelection.end,
            onChange: {
                if onChangeID != 0 {
                    _SUIButtonCallback?(onChangeID)
                }
            },
            onSubmit: {
                if onSubmitID != 0 {
                    _SUIButtonCallback?(onSubmitID)
                }
            }
        )
    }
}

@_cdecl("SUISecureField")
@MainActor
public func SUISecureField(_ placeholderPtr: UnsafePointer<CChar>, _ stateRef: UnsafeMutableRawPointer, _ onSubmitID: UInt) -> UnsafeMutableRawPointer {
    let placeholder = String(cString: placeholderPtr)
    let state = Unmanaged<BridgedStringState>.fromOpaque(stateRef).takeUnretainedValue()
    let view = AnyView(BridgedSecureField(placeholder: placeholder, state: state, onSubmitID: onSubmitID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedSecureField: View {
    let placeholder: String
    let state: BridgedStringState
    let onSubmitID: UInt

    var body: some View {
        @Bindable var bindableState = state
        SUIEditableSecureField(
            placeholder: placeholder,
            text: $bindableState.value,
            selectionStart: nil,
            selectionEnd: nil,
            onChange: nil,
            onSubmit: {
                if onSubmitID != 0 {
                    _SUIButtonCallback?(onSubmitID)
                }
            }
        )
    }
}

@_cdecl("SUISecureFieldCallbacks")
@MainActor
public func SUISecureFieldCallbacks(_ placeholderPtr: UnsafePointer<CChar>, _ stateRef: UnsafeMutableRawPointer, _ onChangeID: UInt, _ onSubmitID: UInt) -> UnsafeMutableRawPointer {
    let placeholder = String(cString: placeholderPtr)
    let state = Unmanaged<BridgedStringState>.fromOpaque(stateRef).takeUnretainedValue()
    let view = AnyView(BridgedSecureFieldCallbacks(placeholder: placeholder, state: state, onChangeID: onChangeID, onSubmitID: onSubmitID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedSecureFieldCallbacks: View {
    let placeholder: String
    let state: BridgedStringState
    let onChangeID: UInt
    let onSubmitID: UInt

    var body: some View {
        @Bindable var bindableState = state
        SUIEditableSecureField(
            placeholder: placeholder,
            text: $bindableState.value,
            selectionStart: nil,
            selectionEnd: nil,
            onChange: {
                if onChangeID != 0 {
                    _SUIButtonCallback?(onChangeID)
                }
            },
            onSubmit: {
                if onSubmitID != 0 {
                    _SUIButtonCallback?(onSubmitID)
                }
            }
        )
    }
}

@_cdecl("SUISecureFieldSelection")
@MainActor
public func SUISecureFieldSelection(_ placeholderPtr: UnsafePointer<CChar>, _ stateRef: UnsafeMutableRawPointer, _ selectionRef: UnsafeMutableRawPointer, _ onSubmitID: UInt) -> UnsafeMutableRawPointer {
    let placeholder = String(cString: placeholderPtr)
    let state = Unmanaged<BridgedStringState>.fromOpaque(stateRef).takeUnretainedValue()
    let selection = Unmanaged<BridgedTextSelectionState>.fromOpaque(selectionRef).takeUnretainedValue()
    let view = AnyView(BridgedSecureFieldSelection(placeholder: placeholder, state: state, selection: selection, onSubmitID: onSubmitID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedSecureFieldSelection: View {
    let placeholder: String
    let state: BridgedStringState
    let selection: BridgedTextSelectionState
    let onSubmitID: UInt

    var body: some View {
        @Bindable var bindableState = state
        @Bindable var bindableSelection = selection
        SUIEditableSecureField(
            placeholder: placeholder,
            text: $bindableState.value,
            selectionStart: $bindableSelection.start,
            selectionEnd: $bindableSelection.end,
            onChange: nil,
            onSubmit: {
                if onSubmitID != 0 {
                    _SUIButtonCallback?(onSubmitID)
                }
            }
        )
    }
}

@_cdecl("SUISecureFieldCallbacksSelection")
@MainActor
public func SUISecureFieldCallbacksSelection(_ placeholderPtr: UnsafePointer<CChar>, _ stateRef: UnsafeMutableRawPointer, _ selectionRef: UnsafeMutableRawPointer, _ onChangeID: UInt, _ onSubmitID: UInt) -> UnsafeMutableRawPointer {
    let placeholder = String(cString: placeholderPtr)
    let state = Unmanaged<BridgedStringState>.fromOpaque(stateRef).takeUnretainedValue()
    let selection = Unmanaged<BridgedTextSelectionState>.fromOpaque(selectionRef).takeUnretainedValue()
    let view = AnyView(BridgedSecureFieldCallbacksSelection(placeholder: placeholder, state: state, selection: selection, onChangeID: onChangeID, onSubmitID: onSubmitID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedSecureFieldCallbacksSelection: View {
    let placeholder: String
    let state: BridgedStringState
    let selection: BridgedTextSelectionState
    let onChangeID: UInt
    let onSubmitID: UInt

    var body: some View {
        @Bindable var bindableState = state
        @Bindable var bindableSelection = selection
        SUIEditableSecureField(
            placeholder: placeholder,
            text: $bindableState.value,
            selectionStart: $bindableSelection.start,
            selectionEnd: $bindableSelection.end,
            onChange: {
                if onChangeID != 0 {
                    _SUIButtonCallback?(onChangeID)
                }
            },
            onSubmit: {
                if onSubmitID != 0 {
                    _SUIButtonCallback?(onSubmitID)
                }
            }
        )
    }
}

@_cdecl("SUITextEditor")
@MainActor
public func SUITextEditor(_ stateRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let state = Unmanaged<BridgedStringState>.fromOpaque(stateRef).takeUnretainedValue()
    let view = AnyView(BridgedTextEditor(state: state))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedTextEditor: View {
    let state: BridgedStringState

    var body: some View {
        @Bindable var bindableState = state
        SUIEditableTextEditor(
            text: $bindableState.value,
            selectionStart: nil,
            selectionEnd: nil,
            onChange: nil,
            onSubmit: nil
        )
    }
}

@_cdecl("SUITextEditorOnChange")
@MainActor
public func SUITextEditorOnChange(_ stateRef: UnsafeMutableRawPointer, _ onChangeID: UInt) -> UnsafeMutableRawPointer {
    let state = Unmanaged<BridgedStringState>.fromOpaque(stateRef).takeUnretainedValue()
    let view = AnyView(BridgedTextEditorOnChange(state: state, onChangeID: onChangeID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedTextEditorOnChange: View {
    let state: BridgedStringState
    let onChangeID: UInt

    var body: some View {
        @Bindable var bindableState = state
        SUIEditableTextEditor(
            text: $bindableState.value,
            selectionStart: nil,
            selectionEnd: nil,
            onChange: {
                if onChangeID != 0 {
                    _SUIButtonCallback?(onChangeID)
                }
            },
            onSubmit: nil
        )
    }
}

@_cdecl("SUITextEditorSelection")
@MainActor
public func SUITextEditorSelection(_ stateRef: UnsafeMutableRawPointer, _ selectionRef: UnsafeMutableRawPointer) -> UnsafeMutableRawPointer {
    let state = Unmanaged<BridgedStringState>.fromOpaque(stateRef).takeUnretainedValue()
    let selection = Unmanaged<BridgedTextSelectionState>.fromOpaque(selectionRef).takeUnretainedValue()
    let view = AnyView(BridgedTextEditorSelection(state: state, selection: selection))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedTextEditorSelection: View {
    let state: BridgedStringState
    let selection: BridgedTextSelectionState

    var body: some View {
        @Bindable var bindableState = state
        @Bindable var bindableSelection = selection
        SUIEditableTextEditor(
            text: $bindableState.value,
            selectionStart: $bindableSelection.start,
            selectionEnd: $bindableSelection.end,
            onChange: nil,
            onSubmit: nil
        )
    }
}

@_cdecl("SUITextEditorOnChangeSelection")
@MainActor
public func SUITextEditorOnChangeSelection(_ stateRef: UnsafeMutableRawPointer, _ selectionRef: UnsafeMutableRawPointer, _ onChangeID: UInt) -> UnsafeMutableRawPointer {
    let state = Unmanaged<BridgedStringState>.fromOpaque(stateRef).takeUnretainedValue()
    let selection = Unmanaged<BridgedTextSelectionState>.fromOpaque(selectionRef).takeUnretainedValue()
    let view = AnyView(BridgedTextEditorOnChangeSelection(state: state, selection: selection, onChangeID: onChangeID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedTextEditorOnChangeSelection: View {
    let state: BridgedStringState
    let selection: BridgedTextSelectionState
    let onChangeID: UInt

    var body: some View {
        @Bindable var bindableState = state
        @Bindable var bindableSelection = selection
        SUIEditableTextEditor(
            text: $bindableState.value,
            selectionStart: $bindableSelection.start,
            selectionEnd: $bindableSelection.end,
            onChange: {
                if onChangeID != 0 {
                    _SUIButtonCallback?(onChangeID)
                }
            },
            onSubmit: nil
        )
    }
}


// MARK: - AppKit-backed editable string controls

private struct SUISelectionRange {
    let start: Int
    let end: Int
}

private func suiNormalizeSelection(_ start: Int, _ end: Int) -> SUISelectionRange {
    let lower = max(0, min(start, end))
    let upper = max(lower, max(start, end))
    return SUISelectionRange(start: lower, end: upper)
}

private func suiClampSelection(
    _ selectionStart: Binding<Int>?,
    _ selectionEnd: Binding<Int>?,
    text: String
) -> NSRange? {
    guard let selectionStart, let selectionEnd else {
        return nil
    }
    let normalized = suiNormalizeSelection(selectionStart.wrappedValue, selectionEnd.wrappedValue)
    let limit = text.utf16.count
    let clamped = SUISelectionRange(start: min(normalized.start, limit), end: min(normalized.end, limit))
    if selectionStart.wrappedValue != clamped.start {
        selectionStart.wrappedValue = clamped.start
    }
    if selectionEnd.wrappedValue != clamped.end {
        selectionEnd.wrappedValue = clamped.end
    }
    return NSRange(location: clamped.start, length: clamped.end - clamped.start)
}

private func suiSyncSelection(_ range: NSRange, start: Binding<Int>?, end: Binding<Int>?) {
    guard let start, let end else {
        return
    }
    let lower = max(0, range.location)
    let upper = max(lower, range.location + range.length)
    if start.wrappedValue != lower {
        start.wrappedValue = lower
    }
    if end.wrappedValue != upper {
        end.wrappedValue = upper
    }
}

@MainActor
private func suiApplyTextFieldStyle(_ field: NSTextField, style: Int32) {
    switch style {
    case 2:
        field.isBezeled = false
        field.isBordered = false
        field.drawsBackground = false
    default:
        field.isBezeled = true
        field.isBordered = true
        field.drawsBackground = true
        field.bezelStyle = .roundedBezel
    }
}

@MainActor
final class SUIEditableFieldCoordinator: NSObject, NSTextFieldDelegate {
    var text: Binding<String>
    var selectionStart: Binding<Int>?
    var selectionEnd: Binding<Int>?
    var onChange: (() -> Void)?
    var onSubmit: (() -> Void)?
    var applying = false
    var pendingSelection: NSRange?

    init(
        text: Binding<String>,
        selectionStart: Binding<Int>?,
        selectionEnd: Binding<Int>?,
        onChange: (() -> Void)?,
        onSubmit: (() -> Void)?
    ) {
        self.text = text
        self.selectionStart = selectionStart
        self.selectionEnd = selectionEnd
        self.onChange = onChange
        self.onSubmit = onSubmit
    }

    func update(
        text: Binding<String>,
        selectionStart: Binding<Int>?,
        selectionEnd: Binding<Int>?,
        onChange: (() -> Void)?,
        onSubmit: (() -> Void)?
    ) {
        self.text = text
        self.selectionStart = selectionStart
        self.selectionEnd = selectionEnd
        self.onChange = onChange
        self.onSubmit = onSubmit
    }

    func apply(to field: NSTextField) {
        let value = text.wrappedValue
        if field.stringValue != value {
            applying = true
            field.stringValue = value
            applying = false
        }
        applySelection(to: field)
    }

    func applySelection(to field: NSTextField) {
        guard let range = suiClampSelection(selectionStart, selectionEnd, text: field.stringValue) else {
            return
        }
        if let editor = field.currentEditor() as? NSTextView {
            if !NSEqualRanges(editor.selectedRange(), range) {
                editor.setSelectedRange(range)
            }
            pendingSelection = nil
            return
        }
        pendingSelection = range
    }

    func sync(from field: NSTextField) {
        if applying {
            return
        }
        let value = field.stringValue
        if text.wrappedValue != value {
            text.wrappedValue = value
            onChange?()
        }
        if let editor = field.currentEditor() as? NSTextView {
            suiSyncSelection(editor.selectedRange(), start: selectionStart, end: selectionEnd)
        }
    }

    func controlTextDidBeginEditing(_ notification: Notification) {
        guard let field = notification.object as? NSTextField else { return }
        if let pendingSelection, let editor = field.currentEditor() as? NSTextView {
            editor.setSelectedRange(pendingSelection)
            self.pendingSelection = nil
        }
        sync(from: field)
    }

    func controlTextDidChange(_ notification: Notification) {
        guard let field = notification.object as? NSTextField else { return }
        sync(from: field)
    }

    func controlTextDidEndEditing(_ notification: Notification) {
        guard let field = notification.object as? NSTextField else { return }
        sync(from: field)
    }

    @objc func submit(_ sender: Any?) {
        if let field = sender as? NSTextField {
            sync(from: field)
        }
        onSubmit?()
    }
}

@MainActor
private struct SUIEditableField<Control: NSTextField>: NSViewRepresentable {
    let placeholder: String
    @Binding var text: String
    let selectionStart: Binding<Int>?
    let selectionEnd: Binding<Int>?
    let onChange: (() -> Void)?
    let onSubmit: (() -> Void)?
    let makeControl: () -> Control

    @Environment(\.suiTextFieldStyleKind) private var style

    func makeCoordinator() -> SUIEditableFieldCoordinator {
        SUIEditableFieldCoordinator(
            text: $text,
            selectionStart: selectionStart,
            selectionEnd: selectionEnd,
            onChange: onChange,
            onSubmit: onSubmit
        )
    }

    func makeNSView(context: Context) -> Control {
        let control = makeControl()
        control.placeholderString = placeholder
        control.delegate = context.coordinator
        control.target = context.coordinator
        control.action = #selector(SUIEditableFieldCoordinator.submit(_:))
        control.focusRingType = .default
        control.controlSize = .regular
        control.setContentCompressionResistancePriority(.defaultLow, for: .horizontal)
        suiApplyTextFieldStyle(control, style: style)
        context.coordinator.apply(to: control)
        return control
    }

    func updateNSView(_ control: Control, context: Context) {
        control.placeholderString = placeholder
        context.coordinator.update(
            text: $text,
            selectionStart: selectionStart,
            selectionEnd: selectionEnd,
            onChange: onChange,
            onSubmit: onSubmit
        )
        suiApplyTextFieldStyle(control, style: style)
        context.coordinator.apply(to: control)
    }
}

@MainActor
struct SUIEditableTextField: View {
    let placeholder: String
    @Binding var text: String
    let selectionStart: Binding<Int>?
    let selectionEnd: Binding<Int>?
    let onChange: (() -> Void)?
    let onSubmit: (() -> Void)?

    var body: some View {
        SUIEditableField(
            placeholder: placeholder,
            text: $text,
            selectionStart: selectionStart,
            selectionEnd: selectionEnd,
            onChange: onChange,
            onSubmit: onSubmit,
            makeControl: { NSTextField(string: "") }
        )
    }
}

@MainActor
struct SUIEditableSecureField: View {
    let placeholder: String
    @Binding var text: String
    let selectionStart: Binding<Int>?
    let selectionEnd: Binding<Int>?
    let onChange: (() -> Void)?
    let onSubmit: (() -> Void)?

    var body: some View {
        SUIEditableField(
            placeholder: placeholder,
            text: $text,
            selectionStart: selectionStart,
            selectionEnd: selectionEnd,
            onChange: onChange,
            onSubmit: onSubmit,
            makeControl: { NSSecureTextField(string: "") }
        )
    }
}

@MainActor
final class SUIEditableTextEditorCoordinator: NSObject, NSTextViewDelegate {
    var text: Binding<String>
    var selectionStart: Binding<Int>?
    var selectionEnd: Binding<Int>?
    var onChange: (() -> Void)?
    var applying = false

    init(
        text: Binding<String>,
        selectionStart: Binding<Int>?,
        selectionEnd: Binding<Int>?,
        onChange: (() -> Void)?
    ) {
        self.text = text
        self.selectionStart = selectionStart
        self.selectionEnd = selectionEnd
        self.onChange = onChange
    }

    func update(
        text: Binding<String>,
        selectionStart: Binding<Int>?,
        selectionEnd: Binding<Int>?,
        onChange: (() -> Void)?
    ) {
        self.text = text
        self.selectionStart = selectionStart
        self.selectionEnd = selectionEnd
        self.onChange = onChange
    }

    func apply(to textView: NSTextView) {
        let value = text.wrappedValue
        if textView.string != value {
            applying = true
            textView.string = value
            applying = false
        }
        if let range = suiClampSelection(selectionStart, selectionEnd, text: textView.string),
            !NSEqualRanges(textView.selectedRange(), range)
        {
            textView.setSelectedRange(range)
        }
    }

    func sync(from textView: NSTextView) {
        if applying {
            return
        }
        let value = textView.string
        if text.wrappedValue != value {
            text.wrappedValue = value
            onChange?()
        }
        suiSyncSelection(textView.selectedRange(), start: selectionStart, end: selectionEnd)
    }

    func textDidChange(_ notification: Notification) {
        guard let textView = notification.object as? NSTextView else { return }
        sync(from: textView)
    }

    func textViewDidChangeSelection(_ notification: Notification) {
        guard let textView = notification.object as? NSTextView else { return }
        suiSyncSelection(textView.selectedRange(), start: selectionStart, end: selectionEnd)
    }
}

@MainActor
private func suiConfigureTextEditorBackground(
    _ scrollView: NSScrollView,
    _ textView: NSTextView,
    hidden: Bool
) {
    scrollView.borderType = hidden ? .noBorder : .bezelBorder
    scrollView.drawsBackground = !hidden
    textView.drawsBackground = !hidden
    if hidden {
        scrollView.backgroundColor = .clear
        textView.backgroundColor = .clear
    } else {
        scrollView.backgroundColor = .textBackgroundColor
        textView.backgroundColor = .textBackgroundColor
    }
}

@MainActor
struct SUIEditableTextEditor: NSViewRepresentable {
    @Binding var text: String
    let selectionStart: Binding<Int>?
    let selectionEnd: Binding<Int>?
    let onChange: (() -> Void)?
    let onSubmit: (() -> Void)?

    @Environment(\.suiScrollContentBackgroundHidden) private var hidesBackground

    func makeCoordinator() -> SUIEditableTextEditorCoordinator {
        SUIEditableTextEditorCoordinator(
            text: $text,
            selectionStart: selectionStart,
            selectionEnd: selectionEnd,
            onChange: onChange
        )
    }

    func makeNSView(context: Context) -> NSScrollView {
        let scrollView = NSScrollView()
        scrollView.hasVerticalScroller = true
        scrollView.hasHorizontalScroller = false
        scrollView.autohidesScrollers = true

        let textView = NSTextView()
        textView.delegate = context.coordinator
        textView.isRichText = false
        textView.importsGraphics = false
        textView.usesAdaptiveColorMappingForDarkAppearance = true
        textView.usesFindPanel = true
        textView.allowsUndo = true
        textView.isVerticallyResizable = true
        textView.isHorizontallyResizable = false
        textView.autoresizingMask = [.width]
        textView.textContainerInset = NSSize(width: 6, height: 8)
        textView.textContainer?.containerSize = NSSize(width: 0, height: CGFloat.greatestFiniteMagnitude)
        textView.textContainer?.widthTracksTextView = true
        scrollView.documentView = textView

        suiConfigureTextEditorBackground(scrollView, textView, hidden: hidesBackground)
        context.coordinator.apply(to: textView)
        return scrollView
    }

    func updateNSView(_ scrollView: NSScrollView, context: Context) {
        guard let textView = scrollView.documentView as? NSTextView else { return }
        context.coordinator.update(
            text: $text,
            selectionStart: selectionStart,
            selectionEnd: selectionEnd,
            onChange: onChange
        )
        suiConfigureTextEditorBackground(scrollView, textView, hidden: hidesBackground)
        context.coordinator.apply(to: textView)
    }
}


@_cdecl("SUISlider")
@MainActor
public func SUISlider(_ labelPtr: UnsafePointer<CChar>, _ stateRef: UnsafeMutableRawPointer, _ min: Double, _ max: Double, _ callbackID: UInt) -> UnsafeMutableRawPointer {
    let label = String(cString: labelPtr)
    let state = Unmanaged<BridgedIntState>.fromOpaque(stateRef).takeUnretainedValue()
    let view = AnyView(BridgedSlider(label: label, state: state, minValue: min, maxValue: max, callbackID: callbackID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedSlider: View {
    let label: String
    var state: BridgedIntState
    let minValue: Double
    let maxValue: Double
    let callbackID: UInt

    var body: some View {
        Slider(value: Binding(
            get: { Double(state.value) },
            set: { state.setAndBump(Int($0)) }
        ), in: minValue...maxValue) {
            Text(label)
        } onEditingChanged: { editing in
            if !editing {
                _SUIButtonCallback?(callbackID)
            }
        }
    }
}

@_cdecl("SUIPickerSegmented")
@MainActor
public func SUIPickerSegmented(_ labelPtr: UnsafePointer<CChar>, _ stateRef: UnsafeMutableRawPointer, _ optionsRef: UnsafeMutableRawPointer, _ callbackID: UInt) -> UnsafeMutableRawPointer {
    let label = String(cString: labelPtr)
    let state = Unmanaged<BridgedIntState>.fromOpaque(stateRef).takeUnretainedValue()
    let options = Unmanaged<Box<AnyView>>.fromOpaque(optionsRef).takeUnretainedValue().value
    let view = AnyView(BridgedPicker(label: label, state: state, options: options, callbackID: callbackID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedPicker: View {
    let label: String
    var state: BridgedIntState
    let options: AnyView
    let callbackID: UInt

    var body: some View {
        Picker(label, selection: Binding(
            get: { state.value },
            set: { newValue in
                state.setAndBump(newValue)
                _SUIButtonCallback?(callbackID)
            }
        )) {
            options
        }
        .pickerStyle(.segmented)
    }
}

@_cdecl("SUIStepperInt")
@MainActor
public func SUIStepperInt(_ labelPtr: UnsafePointer<CChar>, _ stateRef: UnsafeMutableRawPointer, _ min: Int32, _ max: Int32, _ callbackID: UInt) -> UnsafeMutableRawPointer {
    let label = String(cString: labelPtr)
    let state = Unmanaged<BridgedIntState>.fromOpaque(stateRef).takeUnretainedValue()
    let view = AnyView(BridgedStepper(label: label, state: state, minValue: Int(min), maxValue: Int(max), callbackID: callbackID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedStepper: View {
    let label: String
    var state: BridgedIntState
    let minValue: Int
    let maxValue: Int
    let callbackID: UInt

    var body: some View {
        Stepper(label, value: Binding(
            get: { state.value },
            set: { newValue in
                state.setAndBump(newValue)
                _SUIButtonCallback?(callbackID)
            }
        ), in: minValue...maxValue)
    }
}

@_cdecl("SUIPickerMenu")
@MainActor
public func SUIPickerMenu(_ labelPtr: UnsafePointer<CChar>, _ stateRef: UnsafeMutableRawPointer, _ optionsRef: UnsafeMutableRawPointer, _ callbackID: UInt) -> UnsafeMutableRawPointer {
    let label = String(cString: labelPtr)
    let state = Unmanaged<BridgedIntState>.fromOpaque(stateRef).takeUnretainedValue()
    let options = Unmanaged<Box<AnyView>>.fromOpaque(optionsRef).takeUnretainedValue().value
    let view = AnyView(BridgedPickerMenu(label: label, state: state, options: options, callbackID: callbackID))
    return Unmanaged.passRetained(Box(view)).toOpaque()
}

@MainActor
struct BridgedPickerMenu: View {
    let label: String
    var state: BridgedIntState
    let options: AnyView
    let callbackID: UInt

    var body: some View {
        Picker(label, selection: Binding(
            get: { state.value },
            set: { newValue in
                state.setAndBump(newValue)
                _SUIButtonCallback?(callbackID)
            }
        )) {
            options
        }
        .pickerStyle(.menu)
    }
}
