// Code generated by applegen; DO NOT EDIT.

package translation

import (
	"crypto/sha256"
	"encoding/hex"
	"fmt"
	"os"
	"os/exec"
	"path/filepath"
	"runtime"
	"strings"

	"github.com/tmc/swiftui/internal/bridgeutil"

	embeddedbridge "github.com/tmc/swiftui/translation/internal/embeddedbridge"

	"github.com/ebitengine/purego"
)

// libHandle is the handle to the loaded libTranslationSwiftUIBridge dylib.
var libHandle uintptr
var loadErr error

// Err reports the error encountered while loading the Translation bridge, or
// nil if the bridge loaded successfully.
func Err() error { return loadErr }

// Available reports whether the Translation bridge dylib loaded successfully.
func Available() bool { return libHandle != 0 && loadErr == nil }

// swiftBridgeDir returns the path to the vendored Swift bridge source directory.
func swiftBridgeDir() string {
	_, file, _, _ := runtime.Caller(0)
	return filepath.Join(filepath.Dir(file), "internal/swift")
}

// discoverBuiltDylib locates a built bridge dylib across SwiftPM layouts.
func discoverBuiltDylib() (string, error) {
	path, err := bridgeutil.DiscoverBuiltDylib(swiftBridgeDir(), "libTranslationSwiftUIBridge.dylib")
	if err != nil {
		return "", fmt.Errorf("translation: %w", err)
	}
	return path, nil
}

// buildSwiftBridge builds the vendored Swift bridge dylib if needed.
func buildSwiftBridge() (string, error) {
	return bridgeutil.BuildSwiftBridge(swiftBridgeDir(), "libTranslationSwiftUIBridge.dylib", "translation")
}

// materializeEmbeddedBridge writes the embedded bridge payload to a cache directory.
func materializeEmbeddedBridge(data []byte, name string) (string, error) {
	if len(data) == 0 {
		return "", nil
	}
	if name == "" {
		name = "libTranslationSwiftUIBridge.dylib"
	}
	sum := sha256.Sum256(data)
	cacheDir := filepath.Join(os.TempDir(), "translation-bridge-cache")
	if err := os.MkdirAll(cacheDir, 0o755); err != nil {
		return "", fmt.Errorf("translation: create bridge cache: %w", err)
	}
	path := filepath.Join(cacheDir, hex.EncodeToString(sum[:])+"-"+name)
	if _, err := os.Stat(path); err == nil {
		return path, nil
	}
	tmp := path + ".tmp"
	if err := os.WriteFile(tmp, data, 0o755); err != nil {
		return "", fmt.Errorf("translation: write embedded bridge: %w", err)
	}
	if err := os.Rename(tmp, path); err != nil {
		if _, statErr := os.Stat(path); statErr == nil {
			_ = os.Remove(tmp)
			return path, nil
		}
		return "", fmt.Errorf("translation: finalize embedded bridge: %w", err)
	}
	return path, nil
}

// loadEmbeddedBridge attempts to load the bridge from an embedded payload.
func loadEmbeddedBridge() (uintptr, error) {
	data, name := embeddedbridge.Payload()
	if len(data) == 0 {
		return 0, nil
	}
	path, err := materializeEmbeddedBridge(data, name)
	if err != nil {
		return 0, err
	}
	handle, err := purego.Dlopen(path, purego.RTLD_LAZY|purego.RTLD_GLOBAL)
	if err != nil {
		return 0, fmt.Errorf("translation: dlopen embedded bridge %s: %w", path, err)
	}
	return handle, nil
}

// tryRegisterLibFunc attempts to register a C function from the dylib.
func tryRegisterLibFunc(fptr any, handle uintptr, name string) bool {
	defer func() { recover() }()
	purego.RegisterLibFunc(fptr, handle, name)
	return true
}

// C function variables registered via purego.RegisterLibFunc.
var (
	_TRS_TranslationPresentation func(uintptr, *byte) uintptr
	_TRS_Retain                  func(uintptr) uintptr
	_TRS_Release                 func(uintptr)
	_TRS_FreeString              func(*byte)
)

func init() {
	envKey := "LIB" + strings.ToUpper("TranslationSwiftUIBridge") + "_PATH"
	var lastErr error

	if path := os.Getenv(envKey); path != "" {
		var err error
		libHandle, err = purego.Dlopen(path, purego.RTLD_LAZY|purego.RTLD_GLOBAL)
		if err != nil {
			lastErr = err
		}
	}

	if libHandle == 0 {
		if path, err := discoverBuiltDylib(); err == nil {
			libHandle, err = purego.Dlopen(path, purego.RTLD_LAZY|purego.RTLD_GLOBAL)
			if err != nil {
				lastErr = err
			}
		} else {
			lastErr = err
		}
	}

	// Try embedded bridge payload before local Swift build.
	if libHandle == 0 {
		if handle, err := loadEmbeddedBridge(); err == nil && handle != 0 {
			libHandle = handle
		}
	}

	if libHandle == 0 {
		if path, err := buildSwiftBridge(); err == nil {
			libHandle, err = purego.Dlopen(path, purego.RTLD_LAZY|purego.RTLD_GLOBAL)
			if err != nil {
				lastErr = err
			}
		} else {
			lastErr = err
		}
	}

	if libHandle == 0 {
		for _, path := range []string{
			"libTranslationSwiftUIBridge.dylib",
			"/usr/local/lib/libTranslationSwiftUIBridge.dylib",
		} {
			var err error
			libHandle, err = purego.Dlopen(path, purego.RTLD_LAZY|purego.RTLD_GLOBAL)
			if err == nil {
				break
			}
			lastErr = err
		}
	}

	if libHandle == 0 {
		if _, err := exec.LookPath("swift"); err != nil {
			loadErr = fmt.Errorf("translation: swift toolchain not found; install Xcode or Command Line Tools")
		} else if lastErr != nil {
			loadErr = fmt.Errorf("translation: failed to load bridge dylib: %w", lastErr)
		} else {
			loadErr = fmt.Errorf("translation: bridge dylib not loaded")
		}
		setUnavailableStubs()
		return
	}
	tryRegisterLibFunc(&_TRS_TranslationPresentation, libHandle, "TRS_TranslationPresentation")
	tryRegisterLibFunc(&_TRS_Retain, libHandle, "TRS_Retain")
	tryRegisterLibFunc(&_TRS_Release, libHandle, "TRS_Release")
	tryRegisterLibFunc(&_TRS_FreeString, libHandle, "TRS_FreeString")

	setUnavailableStubs()
}

func setUnavailableStubs() {
	stub := func(name string) {
		if loadErr != nil {
			panic("translation: " + name + ": " + loadErr.Error())
		}
		panic("translation: " + name + ": dylib not loaded")
	}
	if _TRS_TranslationPresentation == nil {
		_TRS_TranslationPresentation = func(uintptr, *byte) uintptr { stub("TRS_TranslationPresentation"); return 0 }
	}
	if _TRS_Retain == nil {
		_TRS_Retain = func(uintptr) uintptr { stub("TRS_Retain"); return 0 }
	}
	if _TRS_Release == nil {
		_TRS_Release = func(uintptr) { stub("TRS_Release") }
	}
	if _TRS_FreeString == nil {
		_TRS_FreeString = func(*byte) { stub("TRS_FreeString") }
	}
}
